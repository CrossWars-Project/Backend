name: Generate Daily Crosswords

on:
  schedule:
    # Runs at midnight UTC every day (8 AM UTC = midnight PST)
    - cron: '0 8 * * *'
  
  # Allows manual trigger from GitHub Actions tab
  workflow_dispatch:

jobs:
  generate-crosswords:
    runs-on: ubuntu-latest
    
    steps:
      - name: Generate Daily Crosswords
        run: |
          echo "🎯 Generating daily crosswords at $(date)"
          
          response=$(curl -X POST https://backend-ezw4.onrender.com/crossword/generate-daily \
            -H "Content-Type: application/json" \
            -w "\n%{http_code}" \
            -s \
            --max-time 120)
          
          http_code=$(echo "$response" | tail -n1)
          body=$(echo "$response" | sed '$d')
          
          echo "Response body: $body"
          echo "HTTP status: $http_code"
          
          if [ "$http_code" -eq 200 ]; then
            echo "✅ Daily crosswords generated successfully!"
            exit 0
          else
            echo "❌ Failed to generate crosswords (HTTP $http_code)"
            exit 1
          fi
      
      - name: Summary
        if: success()
        run: echo "✅ Crosswords generated and saved to backend at $(date)"